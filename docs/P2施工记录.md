# P2 五阶段公共接口与课程状态骨架施工记录

阶段/批次：P2 五阶段公共接口与课程状态骨架  
状态：已完成代码与最小 Isaac 验证；未进入 P3 训练。

## 本批变更

- 将课程配置版本和固定观测 schema 升级为 `curriculum-v2-p2`。五个等级分别声明目标/路径分布、六维动作掩码、单一碰撞配置档、辅助奖励缩放、阶段成功门槛、上一等级回归采样比例及已批准随机化清单。
- 新增独立的阶段成功状态机：显式消费位置、姿态、速度、dwell、安全和正式停车成功；安全失败覆盖阶段与正式成功。它不写入 `parking_success`，也不影响最终成功奖励。
- 每个 reset 按 `env_ids` 原子保存课程归属、目标、路径模式和碰撞配置档。上一等级回归采样仅改变该 episode 归属，不污染全局课程控制器当前等级。
- 策略观测接入 P0 冻结的 52 维固定形状：路径模式、碰撞档、净空占位/掩码、动作掩码和课程阶段从阶段 1 起始终存在。
- P2 尚未实现最近点净空：三个净空值与掩码均为零。即使课程档为 C3，也不会将未实现的几何信息误报为已启用；P4 接入并验证距离计算后再启用掩码。
- 课程窗口记录 `curriculum_success`、`formal_parking_success`、安全失败、目标、路径模式和碰撞档。快照同时保存课程配置与观测 schema 版本，缺失或不匹配均拒绝恢复；旧阶段 E checkpoint 不进入 v2 谱系。

## 保持不变的契约

- `configs/task.py` 中的 TCP 标定、四个目标、正式停车阈值、工作空间与安全阈值均未复制或修改。
- `parking_success` 终止和 `final_success` 奖励继续只消费统一停车状态机；课程阶段成功仅用于课程窗口统计。
- Differential IK 数学及控制频率不变。动作掩码目前是已观测、可保存的 P2 契约，P3 才将其接入执行链和路径约束。
- 碰撞配置档当前是每 episode 的身份/观测契约；P3/P4 分别负责实际碰撞档启用与净空几何实现。

## 已执行验证

```powershell
$env:PYTHONNOUSERSITE = '1'
& D:\Anaconda\envs\isaaclab\python.exe -m pytest -q tests/test_curriculum_v2_schema.py tests/test_curriculum_p2_logic.py tests/test_stage_e_logic.py tests/test_stage_e_evaluation.py
& D:\Anaconda\envs\isaaclab\python.exe -m compileall -q source/CurriculumRL/CurriculumRL scripts tests
& D:\Anaconda\envs\isaaclab\python.exe scripts/zero_agent.py --task CurriculumRL-TcpDocking-v0 --num_envs 4 --steps 4 --headless --device cpu
git diff --check
```

- 纯逻辑回归通过：`16 passed`。
- 4 环境、4 步 CPU 零动作 Isaac smoke 正常创建、reset、step 并退出。Kit/PhysX 仍输出既有材质与运动学目标速度警告；本次未出现 Python 异常或 NaN/Inf 终止。
- 未运行 PPO 训练、checkpoint 恢复组合 smoke 或多环境吞吐基准；这些属于 P3/P4 的训练与学习门禁，不构成 P2 通过条件。

## G2 结论与遗留风险

- 已验证：固定 52 维 schema、独立阶段成功、安全优先级、episode 元数据和旧 schema 快照拒绝。
- 尚待 P3：动作掩码实际施加、路径模式几何约束、C1/C2 实体碰撞档切换及其部分 reset smoke。
- 尚待 P4：最近点净空张量、C3 几何验证、`VecNormalize`/课程快照/PPO checkpoint 的成套恢复动态证据。
