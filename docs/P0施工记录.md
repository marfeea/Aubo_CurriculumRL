# P0 决策冻结与可复现基线施工记录

阶段/批次：P0 决策冻结与可复现基线  
状态：已验收；尚未接入训练行为  
事实来源：`docs/课程RL训练施工计划.md` 第 5 节、`docs/课程设计.md`、`configs/task.py`、实际 Isaac 审计输出。

## 本批完成内容

- 保持当前 `stage-e-v1-provisional` 行为配置不变；在 `configs/curriculum.py` 增加未接入训练的 `curriculum-v2-p0` 接口契约。
- 冻结下一代策略观测为 52 维：保留既有 29 维语义，新增路径模式、碰撞配置档、三组净空与掩码、动作掩码、课程阶段。未启用净空严格置零并输出掩码。
- 冻结结果优先级：`safety_failure > formal_parking_success > curriculum_success`；正式奖励仍只绑定统一停车状态机的正式成功。
- 固化路径条件变量 `z`：`direct`、`lateral_positive`、`lateral_negative` 等概率采样；后两者在 B 系中使用 `+0.12 m`、`-0.12 m` 中段参考偏移。P3 才将其接入轨迹约束并以控制与接触 smoke 复验可达性。
- 修正视觉 smoke 对单环境 `CameraSensor.data.info[0]` 的读取；新增单相机审计脚本，输出 RGB、深度、实例分割、实例 metadata、环境系 E 点云范围与 USD 碰撞 API。

## 保持不变的任务契约

- `configs/task.py` 的 TCP 标定、四个目标、最终停车进入/退出距离、姿态/速度/dwell 与安全阈值均未修改或复制。
- 当前 29 维观测、训练入口、checkpoint 谱系和阶段 E 课程运行行为未接入 `curriculum-v2-p0`。
- 相机只在诊断场景中加载；正式非视觉训练场景不增加 `CameraSensor`。

## 已执行验证

```powershell
$env:PYTHONNOUSERSITE = '1'
& D:\Anaconda\envs\isaaclab\python.exe -m compileall -q source/CurriculumRL/CurriculumRL scripts tests
& D:\Anaconda\envs\isaaclab\python.exe -m pytest -q
& D:\Anaconda\envs\isaaclab\python.exe scripts/inspect_scene_semantics.py --headless --device cpu --camera-index 0 --warmup-steps 4 --output data/p0_camera_0_audit.json
& D:\Anaconda\envs\isaaclab\python.exe scripts/inspect_scene_semantics.py --headless --device cpu --camera-index 1 --warmup-steps 4 --output data/p0_camera_1_audit.json
& D:\Anaconda\envs\isaaclab\python.exe scripts/inspect_scene_semantics.py --headless --device cpu --camera-index 2 --warmup-steps 4 --output data/p0_camera_2_audit.json
```

- 三路相机均实际产生审计 JSON。相机 0 识别 `Glass`、`bottle`、`WorkStation`，深度有效像素比 `0.220833`；相机 1 识别同一映射，比例 `0.505280`；外部俯视相机 2 识别工作站，比例 `1.0`。玻璃是透明材质，未出现在相机 2 的深度像素中。
- `data/p0_workstation_usd_audit.json` 确认仅两条工作站 mesh 启用 `PhysicsCollisionAPI`、`PhysxCollisionAPI`、`PhysxTriangleMeshCollisionAPI` 与 `PhysicsMeshCollisionAPI`：`/root/WorkStation/Glass/M_Glass` 与 `/root/WorkStation/WorkStation/M_WorkStation`。

## 运行修复与结论

- Kit 用户配置与 OptiX 缓存隔离到 `data/isaac_kit_cache/`；诊断脚本会从 `sys.path` 排除用户级 Python site-packages，避免用户级 PyTorch 的 `c10.dll` 覆盖 Isaac Lab 环境。
- CUDA 物理设备在本机渲染初始化中不稳定，因此审计采用 `--device cpu`；Kit 的 RTX 渲染仍用于相机输出。这不改变训练设备或任务配置。
- 原始三相机重场景改为三次单相机审计；第三相机由工作站包围盒内部移至外部俯视位。
- C1 冻结为仅 `.../WorkStation/Glass/M_Glass`；C2 冻结为仅 `.../WorkStation/WorkStation/M_WorkStation`；C3 是两者并集。详情见 `docs/课程配置决策记录.md`。

## 准入结论

G0 通过，允许进入 P1。P3 仍须验证三种路径模式的实际可达性；P4/P7 仍须实现并标定最近点净空阈值。
